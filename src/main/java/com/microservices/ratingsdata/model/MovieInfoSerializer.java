package com.microservices.ratingsdata.model;

public class MovieInfoSerializer {//implements BinaryOperator {

/*
    public static void main(String[] args) {
        String abc = null;
        String xyz ="anil";


        Optional<String> optional = Optional.ofNullable(abc);
        Optional<String> optional1 = Optional.ofNullable(xyz);

        //Integer bool = optional.map(Integer.getInteger(abc)).;

        int[] values = {1,3,5,8,6};

        //Stream.of(values).mapToInt()

    }

        @Override
        public void configure(Map<String, ?> map, boolean b) {


            Optional optional = Optional.of("jhjhgjh") ;

        }

        @Override
        public byte[] serialize(String arg0, MovieInfo arg1) {
            byte[] retVal = null;
            ObjectMapper objectMapper = new ObjectMapper();
            try {
                retVal = objectMapper.writeValueAsString(arg1).getBytes();
            } catch (Exception e) {
                e.printStackTrace();
            }
            return retVal;
        }

        @Override
        public void close() {

        }

    @Override
    public Object apply(Object o, Object o2) {

    }*/
}

